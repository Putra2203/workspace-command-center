generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model QueryCache {
  key       String   @id
  value     Json
  expiresAt DateTime @map("expires_at")

  @@map("query_cache")
}
